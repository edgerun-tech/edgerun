id: stream/18-genesis-prev-ref-reject
suite: stream
validator: validate_stream_append
version: 1
priority: mandatory
description: genesis event with predecessor reference is structurally invalid
tags:
- stream
- validate_stream_append
