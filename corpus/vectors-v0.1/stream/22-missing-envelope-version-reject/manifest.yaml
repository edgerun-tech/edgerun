id: stream/22-missing-envelope-version-reject
suite: stream
validator: validate_stream_append
version: 1
priority: mandatory
description: event envelopes must carry envelope_version
tags:
- stream
- validate_stream_append

