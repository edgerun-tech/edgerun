id: stream/20-empty-event-metadata-reject
suite: stream
validator: validate_stream_append
version: 1
priority: mandatory
description: event metadata object reference must carry an object id
tags:
- stream
- validate_stream_append
