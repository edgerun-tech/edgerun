id: stream/23-node-genesis-missing-payload-version-reject
suite: stream
validator: validate_stream_append
version: 1
priority: mandatory
description: node genesis payloads must carry payload_version
tags:
- stream
- validate_stream_append

