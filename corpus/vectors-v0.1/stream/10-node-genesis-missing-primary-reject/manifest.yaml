id: stream/10-node-genesis-missing-primary-reject
suite: stream
validator: validate_stream_append
version: 1
priority: mandatory
description: node genesis event is rejected when required primary node identity is
  absent
tags:
- stream
- validate_stream_append
