id: stream/14-node-genesis-valid-accept
suite: stream
validator: validate_stream_append
version: 1
priority: mandatory
description: node genesis event is accepted when required payload fields are present
tags:
- stream
- validate_stream_append
