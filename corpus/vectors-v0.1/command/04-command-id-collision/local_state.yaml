now: '2030-01-01T00:00:00Z'
local_node: node_server
current_controller_set:
- user_alice
# Replay cache is keyed by command_hash. Same command_id with a different hash
# does NOT cause a collision — the hash is the globally unique key.
replay_cache: {}
policy_profile: default-v0
