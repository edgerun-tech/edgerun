id: delegation/11-empty-link-issuer-reject
suite: delegation
validator: validate_delegation_chain
version: 1
priority: mandatory
description: delegation link with empty issuer is structurally invalid
tags:
- delegation
- validate_delegation_chain
