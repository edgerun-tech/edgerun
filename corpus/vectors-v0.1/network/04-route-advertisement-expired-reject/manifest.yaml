id: network/04-route-advertisement-expired-reject
suite: network
validator: validate_network
version: 1
priority: mandatory
description: route advertisement is rejected when already expired
tags:
- network
- validate_network
