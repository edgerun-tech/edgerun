id: snapshot/06-missing-base-heads-reject
suite: snapshot
validator: validate_snapshot
version: 1
priority: mandatory
description: snapshot without base heads is structurally invalid
tags:
- snapshot
- validate_snapshot
