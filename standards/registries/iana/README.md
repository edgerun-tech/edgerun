# IANA Registries

This directory is for local notes and snapshots of IANA protocol registries.
Do not hand-copy assigned-number tables into protocol files unless the values
are part of an implementation decision. Prefer linking the registry URL from the
protocol record.

Canonical entry point:

```text
https://www.iana.org/protocols
```

