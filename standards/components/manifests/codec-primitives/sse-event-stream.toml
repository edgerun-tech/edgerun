id = "codec-sse-event-stream"
name = "sse-event-stream"
kind = "codec-primitive"
version = "0.1.0"
standard_id = 300072
abi_version = 2
wat = "standards/build/wasm/codec-primitives/sse-event-stream.wat"
runner = "standards/runners/sse-event-stream-smoke.js"
entrypoint = "proto_abi_version"
deterministic = true
inputs = ["bytes", "last-event-id", "record-cap", "text-cap"]
outputs = ["status", "packed_i64", "records", "spans"]
exports = [
  "memory",
  "proto_abi_version",
  "proto_standard_id",
  "sse_parse_events",
]
parity_sources = [
  "crates/utility/edgerun-eventsource-stream/src/lib.rs",
]
