id = "codec-rfc2822-date"
name = "rfc2822-date"
kind = "codec-primitive"
version = "0.1.0"
standard_id = 300054
abi_version = 2
wat = "standards/build/wasm/codec-primitives/rfc2822-date.wat"
runner = "standards/runners/rfc2822-date-smoke.js"
entrypoint = "proto_abi_version"
deterministic = true
inputs = ["scalars"]
outputs = ["status", "packed_i64", "bytes"]
depends_on = []
exports = [
  "memory",
  "proto_abi_version",
  "proto_standard_id",
  "rfc2822_format_utc",
]
notes = "Formats unsigned Unix seconds as fixed UTC RFC 2822 date text: Day, DD Mon YYYY HH:MM:SS +0000. Returns overflow for years beyond the four-digit YYYY surface."
parity_sources = [
  "crates/utility/edgerun-encoding/src/rfc2822.rs",
]
