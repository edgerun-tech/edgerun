id = "codec-http1-body"
name = "http1-body"
kind = "codec-primitive"
version = "0.1.0"
standard_id = 300009
abi_version = 2
wat = "standards/build/wasm/codec-primitives/http1-body.wat"
runner = "standards/runners/http1-body-smoke.js"
entrypoint = "proto_abi_version"
deterministic = true
inputs = ["bytes"]
outputs = ["status", "records"]
depends_on = ["codec-http1-scan"]
exports = [
  "memory",
  "proto_abi_version",
  "proto_standard_id",
  "http_parse_content_length",
  "http_has_transfer_token",
  "http_classify_body_framing",
]
parity_sources = [
  "crates/protocol/edgerun-protocols/src/http/message.rs",
  "crates/protocol/edgerun-protocols/src/http/chunked.rs",
]

