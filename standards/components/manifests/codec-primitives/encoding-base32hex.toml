id = "codec-encoding-base32hex"
name = "encoding-base32hex"
kind = "codec-primitive"
version = "0.1.0"
standard_id = 300052
abi_version = 2
wat = "standards/build/wasm/codec-primitives/encoding-base32hex.wat"
runner = "standards/runners/encoding-base32hex-smoke.js"
entrypoint = "proto_abi_version"
deterministic = true
inputs = ["bytes"]
outputs = ["status", "packed_i64", "bytes"]
depends_on = []
exports = [
  "memory",
  "proto_abi_version",
  "proto_standard_id",
  "base32hex_encode",
  "base32hex_decode",
]
parity_sources = [
  "crates/utility/edgerun-encoding/src/base32hex.rs",
]
