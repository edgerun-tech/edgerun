id = "codec-ssh-authorized-key"
name = "ssh-authorized-key"
kind = "codec-primitive"
version = "0.1.0"
standard_id = 300045
abi_version = 2
wat = "standards/build/wasm/codec-primitives/ssh-authorized-key.wat"
runner = "standards/runners/ssh-authorized-key-smoke.js"
entrypoint = "proto_abi_version"
deterministic = true
inputs = ["bytes"]
outputs = ["status", "records"]
depends_on = []
exports = [
  "memory",
  "proto_abi_version",
  "proto_standard_id",
  "ssh_authorized_key_scan",
]
parity_sources = []
