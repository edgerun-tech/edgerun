id = "codec-tls-name"
name = "tls-name"
kind = "codec-primitive"
version = "0.1.0"
standard_id = 300014
abi_version = 2
wat = "standards/build/wasm/codec-primitives/tls-name.wat"
runner = "standards/runners/tls-name-smoke.js"
entrypoint = "proto_abi_version"
deterministic = true
inputs = ["bytes"]
outputs = ["status", "bytes", "boolean"]
depends_on = []
exports = [
  "memory",
  "proto_abi_version",
  "proto_standard_id",
  "tls_dns_name_normalize",
  "tls_dns_name_matches",
]
parity_sources = [
  "crates/protocol/edgerun-protocols/src/tls/name_match.rs",
]

