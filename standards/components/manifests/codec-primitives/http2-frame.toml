id = "codec-http2-frame"
name = "http2-frame"
kind = "codec-primitive"
version = "0.1.0"
standard_id = 300010
abi_version = 2
wat = "standards/build/wasm/codec-primitives/http2-frame.wat"
runner = "standards/runners/http2-frame-smoke.js"
entrypoint = "proto_abi_version"
deterministic = true
inputs = ["bytes", "scalars"]
outputs = ["status", "records", "bytes"]
depends_on = ["codec-encoding-core"]
exports = [
  "memory",
  "proto_abi_version",
  "proto_standard_id",
  "http2_frame_type_classify",
  "http2_frame_header_decode",
  "http2_frame_header_encode",
]
parity_sources = [
  "crates/protocol/edgerun-protocols/src/http/http2/frame/mod.rs",
]

