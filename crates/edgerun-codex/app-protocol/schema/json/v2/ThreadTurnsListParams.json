{
  "title": "ThreadTurnsListParams",
  "type": "object",
  "properties": {
    "cursor": {
      "type": "string"
    },
    "itemsView": {
      "title": "TurnItemsView",
      "type": "string",
      "enum": [
        "NotLoaded",
        "Summary",
        "Full"
      ]
    },
    "limit": {
      "type": "integer"
    },
    "sortDirection": {
      "title": "SortDirection",
      "type": "string",
      "enum": [
        "Asc",
        "Desc"
      ]
    },
    "threadId": {
      "type": "string"
    }
  },
  "required": [
    "threadId"
  ]
}