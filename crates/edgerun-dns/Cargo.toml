[package]
name = "edgerun-dns"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Async DNS server and client using edgerun-rt"

[dependencies]
edgerun-rt = { path = "../edgerun-rt" }
edgerun-log = { path = "../edgerun-log" }
# edgerun-tls = { path = "../edgerun-tls" }  # TODO: re-enable when edgerun-tls compiles
