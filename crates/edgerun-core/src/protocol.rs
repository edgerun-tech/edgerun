//! Removed stale protocol compatibility surface.
//!
//! The active protocol module is the native protocol tree selected from
//! `lib.rs`. This stale compatibility file is intentionally empty so any direct
//! dependency on it must move to the rkyv boundary.
