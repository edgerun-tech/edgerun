// This file is @generated by prost-build.
/// Execution context for a WASM app instance.
#[derive(Clone, PartialEq)]
pub struct ExecutionContext {
    pub version: u32,
    /// The AppPackage being executed
    pub app_package: ::core::option::Option<super::common::ObjectRef>,
    /// Instance ID for this execution (unique per invocation)
    pub instance_id: alloc::vec::Vec<u8>,
    /// Capabilities granted for this execution
    /// MUST be: intersect(app.required_capabilities, node_policy, delegation_chain)
    pub granted_capabilities: alloc::vec::Vec<super::trust::CapabilityDescriptor>,
    /// Execution class constraint
    pub execution_class: i32,
    /// Domain scope for this execution
    pub domain: alloc::string::String,
}
/// WASM host capability operations — these are checked against
/// granted_capabilities before allowing the operation.
#[derive(Clone, PartialEq)]
pub struct CapabilityCheck {
    pub operation: i32,
    /// Optional scope constraint for the operation
    pub scope: ::core::option::Option<super::trust::ScopeDescriptor>,
}
/// Nested message and enum types in `CapabilityCheck`.
pub mod capability_check {
    #[derive(Clone, Copy, Debug, PartialEq, Eq)]
    #[repr(i32)]
    pub enum Operation {
        Unspecified = 0,
        ReadBlob = 1,
        WriteBlob = 2,
        SendMessage = 3,
        NetworkBind = 4,
        NetworkConnect = 5,
        StorageAccess = 6,
        Execution = 7,
    }
    impl Operation {
        /// String value of the enum field names used in the ProtoBuf definition.
        ///
        /// The values are not transformed in any way and thus are considered stable
        /// (if the ProtoBuf definition does not change) and safe for programmatic use.
        pub fn as_str_name(&self) -> &'static str {
            match self {
                Self::Unspecified => "OPERATION_UNSPECIFIED",
                Self::ReadBlob => "OPERATION_READ_BLOB",
                Self::WriteBlob => "OPERATION_WRITE_BLOB",
                Self::SendMessage => "OPERATION_SEND_MESSAGE",
                Self::NetworkBind => "OPERATION_NETWORK_BIND",
                Self::NetworkConnect => "OPERATION_NETWORK_CONNECT",
                Self::StorageAccess => "OPERATION_STORAGE_ACCESS",
                Self::Execution => "OPERATION_EXECUTION",
            }
        }
        /// Creates an enum from field names used in the ProtoBuf definition.
        pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
            match value {
                "OPERATION_UNSPECIFIED" => Some(Self::Unspecified),
                "OPERATION_READ_BLOB" => Some(Self::ReadBlob),
                "OPERATION_WRITE_BLOB" => Some(Self::WriteBlob),
                "OPERATION_SEND_MESSAGE" => Some(Self::SendMessage),
                "OPERATION_NETWORK_BIND" => Some(Self::NetworkBind),
                "OPERATION_NETWORK_CONNECT" => Some(Self::NetworkConnect),
                "OPERATION_STORAGE_ACCESS" => Some(Self::StorageAccess),
                "OPERATION_EXECUTION" => Some(Self::Execution),
                _ => None,
            }
        }
    }
}

/// Result of a capability check
#[derive(Clone, PartialEq)]
pub struct CapabilityResult {
    pub decision: i32,
    pub reason: alloc::string::String,
}
/// Nested message and enum types in `CapabilityResult`.
pub mod capability_result {
    #[derive(Clone, Copy, Debug, PartialEq, Eq)]
    #[repr(i32)]
    pub enum Decision {
        Unspecified = 0,
        Granted = 1,
        Denied = 2,
    }
    impl Decision {
        /// String value of the enum field names used in the ProtoBuf definition.
        ///
        /// The values are not transformed in any way and thus are considered stable
        /// (if the ProtoBuf definition does not change) and safe for programmatic use.
        pub fn as_str_name(&self) -> &'static str {
            match self {
                Self::Unspecified => "DECISION_UNSPECIFIED",
                Self::Granted => "DECISION_GRANTED",
                Self::Denied => "DECISION_DENIED",
            }
        }
        /// Creates an enum from field names used in the ProtoBuf definition.
        pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
            match value {
                "DECISION_UNSPECIFIED" => Some(Self::Unspecified),
                "DECISION_GRANTED" => Some(Self::Granted),
                "DECISION_DENIED" => Some(Self::Denied),
                _ => None,
            }
        }
    }
}
