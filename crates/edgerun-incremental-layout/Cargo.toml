[package]
name = "edgerun-incremental-layout"
version.workspace = true
edition.workspace = true
publish = false
description = "Layer 11: Incremental layout — recompute only affected subtree on CSS change"

[dependencies]
edgerun-rasterizer = { path = "../edgerun-rasterizer" }

[features]
default = ["std"]
std = []
[lints]
workspace = true
