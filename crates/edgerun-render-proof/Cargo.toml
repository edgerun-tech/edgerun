[package]
name = "edgerun-render-proof"
version.workspace = true
edition.workspace = true
publish = false
description = "Layer 12: Deterministic rendering guarantee — prove CPU = GPU for all fuzz inputs"

[dependencies]
edgerun-rasterizer = { path = "../edgerun-rasterizer" }
edgerun-wgpu = { path = "../edgerun-wgpu" }
png = "0.18"
bytemuck = { version = "1", features = ["derive"] }
pollster = "0.4"
wgpu = "29"

[lints]
workspace = true
