# edgerun-gpu

Cross-platform GPU types, traits, and capability descriptors.

Current scope:

- `GpuInfo` inventory model
- `GpuVendor` classification
- `GpuInventory` trait
- default capability descriptor helpers
- vendor inference from common PCI IDs
