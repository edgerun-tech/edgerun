[package]
name = "edgerun-provisioning"
version = "0.1.0"
edition = "2021"
description = "EdgeRun bootstrap provisioning: single-use contracts, node genesis, controller verification"

[dependencies]
edgerun-core = { path = "../edgerun-core" }
edgerun-crypto = { path = "../edgerun-crypto" }
edgerun-stream = { path = "../edgerun-stream" }
edgerun-error = { path = "../edgerun-error" }
# edgerun-capabilities = { path = "../edgerun-capabilities" }  # Uncomment when needed

[dev-dependencies]
