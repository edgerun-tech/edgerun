[package]
name = "edgerun-wire"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Rkyv-only Edgerun wire protocol boundary"

[features]
default = []
std = []

[dependencies]
edgerun-crypto = { version = "0.1", path = "../../utility/edgerun-crypto", default-features = false, features = ["sha2"] }
rkyv = { version = "0.8.16", default-features = false, features = ["alloc", "bytecheck"] }

[lints]
workspace = true
