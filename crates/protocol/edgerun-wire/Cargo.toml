[package]
name = "edgerun-wire"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Rkyv-only Edgerun wire protocol boundary"

[features]
default = []
std = []

[dependencies]
edgerun-crypto = { workspace = true, default-features = false, features = ["sha2"] }
rkyv = { workspace = true, features = ["alloc", "bytecheck"] }

[lints]
workspace = true
