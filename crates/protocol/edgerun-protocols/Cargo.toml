[package]
name = "edgerun-protocols"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Transport-independent protocol parsers, encoders, and state machines"

[dependencies]
edgerun-crypto = { workspace = true, default-features = false, optional = true }
edgerun-core = { workspace = true, default-features = false, optional = true }
edgerun-encoding = { workspace = true, default-features = false, optional = true }
edgerun-json = { workspace = true, default-features = false, optional = true }
edgerun-tpm = { path = "../../identity/edgerun-tpm", default-features = false, features = ["std"], optional = true }
edgerun-url = { workspace = true, default-features = false, optional = true }
edgerun-wire = { workspace = true, default-features = false, optional = true }

[features]
default = []
acme = ["dep:edgerun-crypto", "dep:edgerun-encoding", "dep:edgerun-json", "dep:edgerun-url", "edgerun-crypto/sha2"]
block = ["dep:edgerun-encoding", "wire"]
bluetooth-gatt = ["dep:edgerun-encoding"]
bluetooth-mgmt = ["dep:edgerun-encoding"]
cec = []
core = ["dep:edgerun-core"]
ed25519 = ["dep:edgerun-crypto", "edgerun-crypto/ed25519"]
proxy = ["dep:edgerun-encoding"]
pxe = []
quectel-ec200a = []
dbus = ["dep:edgerun-encoding"]
dhcp = ["dep:edgerun-encoding"]
dhcpv6 = ["dep:edgerun-encoding"]
dns = ["dep:edgerun-encoding"]
dnssec = ["dns", "dep:edgerun-crypto", "edgerun-crypto/ed25519", "edgerun-crypto/p256", "edgerun-crypto/sha1"]
email-auth = ["dep:edgerun-crypto", "dep:edgerun-encoding", "edgerun-crypto/rsa"]
emrtd = []
ethernet-ipv4 = ["dep:edgerun-encoding"]
goodix-fingerprint = ["dep:edgerun-encoding"]
http = ["dep:edgerun-encoding"]
http2-server = ["http"]
http-compression = ["http", "dep:edgerun-encoding"]
http3 = ["http", "dep:edgerun-encoding"]
imap = ["dep:edgerun-encoding"]
keygen = ["sign-p256"]
lmtp = ["smtp"]
matter = ["dep:edgerun-encoding"]
nbd = ["block", "dep:edgerun-encoding"]
ndef = ["dep:edgerun-encoding"]
node-bootstrap = ["keygen", "sign-p256", "verify"]
oci = []
oauth = ["dep:edgerun-crypto", "dep:edgerun-encoding", "edgerun-crypto/sha2"]
quic = ["tls", "tls-ed25519", "tls-rsa", "tls-x25519"]
seal = ["keygen", "dep:edgerun-crypto", "edgerun-crypto/aead"]
serial-mux = []
sign = ["verify"]
sign-p256 = ["sign"]
smtp = ["dep:edgerun-encoding"]
ssh = ["dep:edgerun-encoding"]
tls = ["tls-cert", "dep:edgerun-encoding", "edgerun-crypto/aead", "edgerun-crypto/hmac"]
tls-tpm = ["tls", "dep:edgerun-tpm", "edgerun-tpm/std"]
tls-cert = ["dep:edgerun-crypto", "edgerun-crypto/p256", "edgerun-crypto/pem", "edgerun-crypto/sha2"]
tls-ed25519 = ["tls", "edgerun-crypto/ed25519"]
tls-rsa = ["tls", "edgerun-crypto/rsa"]
tls-x25519 = ["tls", "edgerun-crypto/x25519"]
tcl-ac = ["dep:edgerun-crypto", "edgerun-crypto/aes", "edgerun-crypto/hmac"]
tsig = ["dns", "dep:edgerun-crypto", "dep:edgerun-encoding", "edgerun-crypto/hmac"]
tftp = ["dep:edgerun-encoding"]
tuya = ["dep:edgerun-crypto", "dep:edgerun-encoding", "dep:edgerun-json", "edgerun-crypto/aead", "edgerun-crypto/hmac"]
usb = ["dep:edgerun-encoding"]
verify = ["core"]
wayland = ["dep:edgerun-encoding"]
websocket = ["dep:edgerun-crypto", "dep:edgerun-encoding", "edgerun-crypto/sha1"]
wifi = ["dep:edgerun-encoding"]
wire = ["dep:edgerun-wire"]

[lints]
workspace = true
