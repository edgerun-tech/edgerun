[package]
name = "edgerun-rule-optimizer"
version.workspace = true
edition.workspace = true
publish = false
description = "Layer 18: CSS rule optimization engine — merge, remove, flatten, and reorder rules"

[dependencies]
edgerun-property-graph = { path = "../edgerun-property-graph" }
