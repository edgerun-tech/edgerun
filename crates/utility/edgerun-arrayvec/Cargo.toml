[workspace]

[package]
name = "edgerun-arrayvec"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Edgerun-owned fixed-capacity array-backed vector (ArrayVec)"

[features]
default = ["alloc"]
alloc = []

[dependencies]

[lib]
path = "src/lib.rs"
