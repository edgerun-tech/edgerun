[package]
name = "edgerun-serde"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Edgerun serde compatibility surface"

[features]
default = []
derive = ["serde/derive"]
alloc = ["serde/alloc"]
rc = ["serde/rc"]
std = ["serde/std"]

[dependencies]
serde = { version = "1", default-features = false }

[lints]
workspace = true
