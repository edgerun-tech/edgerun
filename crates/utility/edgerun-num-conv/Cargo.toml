[workspace]

[package]
name = "edgerun-num-conv"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Edgerun-owned num-conv crate: convert between integer types without `as` casts"

[lib]
path = "src/lib.rs"
