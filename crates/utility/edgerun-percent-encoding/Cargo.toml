[workspace]

[package]
name = "edgerun-percent-encoding"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Edgerun-owned percent-encoding crate: percent encoding and decoding"

[lib]
path = "src/lib.rs"

[features]
default = ["alloc"]
alloc = []
