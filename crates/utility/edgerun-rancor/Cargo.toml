[package]
name = "rancor"
version = "0.1.1"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "EdgeRun-owned scalable error handling without type composition"

[features]
default = ["alloc"]
alloc = []

[dependencies]
ptr_meta = { path = "../edgerun-ptr-meta", features = ["derive"], default-features = false }

[lints]
workspace = true
