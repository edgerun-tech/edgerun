[workspace]

[package]
name = "edgerun-try-lock"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Edgerun-owned try-lock crate: a light-weight lock guarded by an atomic boolean"

[lib]
path = "src/lib.rs"
