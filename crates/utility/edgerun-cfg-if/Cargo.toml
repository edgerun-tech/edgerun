[workspace]

[package]
name = "edgerun-cfg-if"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Edgerun-owned cfg_if! macro for conditional compilation"

[lib]
path = "src/lib.rs"
