# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.75.0"
name = "opentelemetry"
version = "0.31.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "OpenTelemetry API for Rust"
homepage = "https://github.com/open-telemetry/opentelemetry-rust/tree/main/opentelemetry"
readme = "README.md"
keywords = [
    "opentelemetry",
    "logging",
    "tracing",
    "metrics",
]
categories = [
    "development-tools::debugging",
    "development-tools::profiling",
    "api-bindings",
    "asynchronous",
]
license = "Apache-2.0"
repository = "https://github.com/open-telemetry/opentelemetry-rust/tree/main/opentelemetry"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
    "--cfg",
    "docsrs",
]

[features]
default = [
    "trace",
    "metrics",
    "logs",
    "internal-logs",
    "futures",
]
futures = [
    "futures-core",
    "futures-sink",
    "pin-project-lite",
]
internal-logs = ["tracing"]
logs = []
metrics = []
spec_unstable_logs_enabled = ["logs"]
testing = ["trace"]
trace = [
    "futures",
    "thiserror",
]

[lib]
name = "opentelemetry"
path = "src/lib.rs"
bench = false

[dependencies.futures-core]
path = "../edgerun-futures-core"
optional = true

[dependencies.futures-sink]
path = "../edgerun-futures-sink"
optional = true

[dependencies.pin-project-lite]
path = "../edgerun-pin-project-lite"
optional = true

[dependencies.thiserror]
path = "../edgerun-thiserror"
optional = true
default-features = false

[dependencies.tracing]
path = "../edgerun-tracing"
optional = true
default-features = false

[target.'cfg(all(target_arch = "wasm32", not(target_os = "wasi")))'.dependencies.js-sys]
version = "0.3.63"

[lints.clippy.all]
level = "warn"
priority = 1

[lints.rust]
edition_2024_expr_fragment_specifier = "allow"
if_let_rescope = "allow"
tail_expr_drop_order = "allow"
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(edgerun_upstream_context_tests)"] }

[lints.rust.rust_2024_compatibility]
level = "warn"
priority = -1
