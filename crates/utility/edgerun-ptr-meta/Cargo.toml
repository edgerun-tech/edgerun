[workspace]

[package]
name = "edgerun-ptr-meta"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Edgerun-owned Pointee trait and pointer metadata utilities"

[features]
default = ["std"]
std = []

[dependencies]

[lib]
path = "src/lib.rs"
