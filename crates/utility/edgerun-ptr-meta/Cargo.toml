[package]
name = "ptr_meta"
version = "0.3.1"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "EdgeRun-owned pointer metadata utilities"

[features]
default = ["derive", "std"]
derive = ["ptr_meta_derive"]
std = []

[dependencies]
ptr_meta_derive = { path = "../edgerun-ptr-meta-derive", optional = true, default-features = false }

[lints]
workspace = true
