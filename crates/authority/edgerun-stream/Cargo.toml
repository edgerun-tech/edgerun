[package]
name = "edgerun-stream"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Single-writer stream sequencing, hash-linking, signing, and verification for EdgeRun"

[dependencies]
edgerun-protocols = { workspace = true, default-features = false, features = ["sign-p256", "keygen", "core"] }

[features]
default = []
std = []

[lints]
workspace = true
