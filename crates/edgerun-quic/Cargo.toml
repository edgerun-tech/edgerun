[package]
name = "edgerun-quic"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "QUIC transport protocol (RFC 9000)"

[features]
default = []
std = []

[lints]
workspace = true

[dependencies]
edgerun-tls = { path = "../edgerun-tls", default-features = false }
edgerun-crypto = { path = "../edgerun-crypto", default-features = false, features = ["aead", "ed25519", "hmac", "p256", "rsa"] }
edgerun-rt = { path = "../edgerun-rt" }
edgerun-encoding = { path = "../edgerun-encoding", default-features = false }
