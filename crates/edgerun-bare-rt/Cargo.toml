[package]
name = "edgerun-bare-rt"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
edgerun-platform = { path = "../edgerun-platform" }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1