[package]
name = "edgerun-qpack"
version = "0.1.0"
edition = "2021"
description = "QPACK header compression for HTTP/3 (RFC 9204)"
license = "MIT"

[dependencies]
bytes = "1"

[features]
default = []
http-headers = []
