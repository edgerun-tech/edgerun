[package]
name = "edgerun-qpack"
version = "0.1.0"
edition = "2021"
description = "QPACK header compression for HTTP/3 (RFC 9204)"
license = "MIT"

[dependencies]
edgerun-encoding = { path = "../edgerun-encoding" }

[dev-dependencies]
assert_matches = "1"
proptest = "1"

[features]
default = []
http-headers = []
