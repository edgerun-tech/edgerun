# edgerun-linux-gpu

Linux sysfs/DRM-backed GPU discovery.

## Usage

```bash
cargo run -p edgerun-linux-gpu --bin linux-gpu-tool -- list
cargo run -p edgerun-linux-gpu --bin linux-gpu-tool -- inventory
```
