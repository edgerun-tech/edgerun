[package]
name = "edgerun-proto-watch"
version.workspace = true
edition.workspace = true
publish = false
description = "Hot-reload proto files: edit proto → regenerate → re-render"

[dependencies]
notify = { version = "8", default-features = false }