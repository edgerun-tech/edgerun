//! OCI Linux catalog names are owned by `edgerun-protocols`.

pub(crate) use edgerun_protocols::oci::linux_catalog::{
    LINUX_CAPABILITIES, capability_number, is_oci_namespace_name, namespace_flag, rlimit_number,
    seccomp_arch_bpf,
};
