[package]
name = "edgerun-mesh-link"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Mesh link discovery and transport helpers for Edgerun"

[dependencies]
edgerun-mesh = { path = "../edgerun-mesh" }
edgerun-hardware-signing = { path = "../edgerun-hardware-signing" }

[target.'cfg(not(target_os = "none"))'.dependencies]
libc.workspace = true
edgerun-network-interface = { path = "../edgerun-network-interface" }

[dev-dependencies]
edgerun-core = { path = "../edgerun-core" }
edgerun-crypto = { workspace = true, default-features = false, features = ["p256"] }
edgerun-mesh = { path = "../edgerun-mesh" }

[lints]
workspace = true
