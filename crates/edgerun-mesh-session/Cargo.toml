[package]
name = "edgerun-mesh-session"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Authenticated mesh session establishment for Edgerun"

[dependencies]
edgerun-mesh = { version = "0.1", path = "../edgerun-mesh" }
edgerun-hardware-signing = { version = "0.1", path = "../edgerun-hardware-signing" }
edgerun-crypto = { workspace = true, default-features = false, features = ["aead", "p256"] }
edgerun-core = { version = "0.1", path = "../protocol/edgerun-core" }

[lints]
workspace = true
