[package]
name = "edgerun-git"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Small host-only Git repository explorer with in-repository visibility controls"

[dependencies]
edgerun-http = { version = "0.1", path = "../edgerun-http", default-features = false, features = ["server"] }
edgerun-log = { version = "0.1", path = "../edgerun-log" }
edgerun-rt = { version = "0.1", path = "../edgerun-rt" }

[lints]
workspace = true
