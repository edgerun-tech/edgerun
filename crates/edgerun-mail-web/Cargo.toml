[package]
name = "edgerun-mail-web"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Webmail frontend for edgerun email server"

[dependencies]
edgerun-http = { version = "0.1", path = "../edgerun-http", default-features = false, features = ["server", "static-files"] }
edgerun-log = { version = "0.1", path = "../edgerun-log" }

[lints]
workspace = true
