diff --git a/crates/edgerun-compositor/src/render/server.rs b/crates/edgerun-compositor/src/render/server.rs
index 0000000..0000000 100644
--- a/crates/edgerun-compositor/src/render/server.rs
+++ b/crates/edgerun-compositor/src/render/server.rs
@@ -170,7 +170,10 @@ pub fn render_and_flip(
         }
     }
 
-    // Draw cursor
+    // Draw native EdgeRun system UI above Wayland surfaces and below cursor.
+    crate::render::system_ui::draw_system_ui_overlay(pixels, width, height, stride);
+
+    // Draw cursor
     cursor.draw(pixels, width, height, stride);
 
     // Page flip to display the rendered buffer
