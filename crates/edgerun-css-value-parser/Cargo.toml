[package]
name = "edgerun-css-value-parser"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "CSS value type parser — converts CSS value strings into typed values"

[dependencies]
edgerun-color = { path = "../edgerun-color" }
