[package]
name = "edgerun-proxy"
version.workspace = true
edition.workspace = true
description = "HTTP proxy server with CONNECT support for HTTPS tunneling"

[dependencies]
edgerun-rt = { path = "../edgerun-rt" }
edgerun-log = { path = "../edgerun-log" }

[features]
default = []
