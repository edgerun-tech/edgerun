[package]
name = "edgerun-ssh"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Built-in SSH transport primitives for Edgerun deployment"

[features]
default = ["std"]
std = []

[dependencies]
edgerun-crypto = { workspace = true, default-features = false, features = ["aes", "hmac", "x25519"] }
edgerun-encoding = { workspace = true }
edgerun-sign = { workspace = true, default-features = false, features = ["ed25519"] }
edgerun-verify = { workspace = true, default-features = false, features = ["ed25519"] }

[lints]
workspace = true
