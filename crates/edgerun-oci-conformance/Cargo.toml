[package]
name = "edgerun-oci-conformance"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "OCI runtime conformance tests — cgroup v2 aware, native Rust"

[dev-dependencies]
edgerun-oci-runtime = { path = "../edgerun-oci-runtime" }
edgerun-json = { path = "../edgerun-json" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
