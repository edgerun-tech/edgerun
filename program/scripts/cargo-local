#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

export CARGO_HOME="$ROOT_DIR/.cargo-home"
export CARGO_INSTALL_ROOT="$ROOT_DIR/.cargo"
export CARGO_TARGET_DIR="$ROOT_DIR/target"
export PATH="$ROOT_DIR/.cargo/bin:$PATH"

exec cargo "$@"
