syntax = "proto3";

package edgerun.v0.ecmascript.objects;

/// ECMAScript Built-in Object Definitions — generated from ECMA-262.
/// DO NOT EDIT. Regenerate with: scripts/generate_ecmascript_proto.py

/// All ECMAScript built-in objects.
/// Total: 27 objects.
enum JsBuiltInObject {
  JS_BUILT_IN_OBJECT_UNSPECIFIED = 0;
  OBJECT = 1; // Object (6 methods, 1 static props)
  FUNCTION = 2; // Function (4 methods, 1 static props)
  BOOLEAN = 3; // Boolean (2 methods, 1 static props)
  SYMBOL = 4; // Symbol (2 methods, 14 static props)
  ERROR = 5; // Error (1 methods, 1 static props)
  NUMBER = 6; // Number (6 methods, 9 static props)
  BIG_INT = 7; // BigInt (3 methods, 1 static props)
  DATE = 8; // Date (46 methods, 1 static props)
  STRING = 9; // String (50 methods, 1 static props)
  REG_EXP = 10; // RegExp (4 methods, 1 static props)
  ARRAY = 11; // Array (38 methods, 1 static props)
  UINT8_ARRAY = 12; // Uint8Array (4 methods, 0 static props)
  MAP = 13; // Map (11 methods, 1 static props)
  SET = 14; // Set (15 methods, 1 static props)
  WEAK_MAP = 15; // WeakMap (6 methods, 1 static props)
  WEAK_SET = 16; // WeakSet (3 methods, 1 static props)
  ARRAY_BUFFER = 17; // ArrayBuffer (4 methods, 1 static props)
  SHARED_ARRAY_BUFFER = 18; // SharedArrayBuffer (2 methods, 1 static props)
  DATA_VIEW = 19; // DataView (22 methods, 1 static props)
  WEAK_REF = 20; // WeakRef (1 methods, 1 static props)
  FINALIZATION_REGISTRY = 21; // FinalizationRegistry (2 methods, 1 static props)
  ITERATOR = 22; // Iterator (11 methods, 1 static props)
  PROMISE = 23; // Promise (3 methods, 1 static props)
  AGGREGATE_ERROR = 24; // AggregateError (0 methods, 1 static props)
  GENERATOR_FUNCTION = 25; // GeneratorFunction (0 methods, 1 static props)
  ASYNC_GENERATOR_FUNCTION = 26; // AsyncGeneratorFunction (0 methods, 1 static props)
  ASYNC_FUNCTION = 27; // AsyncFunction (0 methods, 1 static props)
}

/// An ECMAScript built-in method.
message JsMethod {
  string name = 1;
  repeated string parameters = 2;
  string section = 3;
  bool is_prototype_method = 4;
  bool is_static = 5;
}

/// An ECMAScript built-in property.
message JsProperty {
  string name = 1;
  string kind = 2; // "data", "get", "set"
  string section = 3;
}

/// Complete definition of an ECMAScript built-in object.
message JsBuiltInObjectDef {
  JsBuiltInObject object = 1;
  string name = 2;
  repeated JsMethod prototype_methods = 3;
  repeated JsMethod static_methods = 4;
  repeated JsProperty prototype_properties = 5;
  repeated JsProperty static_properties = 6;
  string section = 7;
}
