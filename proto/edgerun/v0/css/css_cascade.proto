syntax = "proto3";

package edgerun.v0.css.cascade;

/// CSS Cascade Types — generated from CSS Cascade Level 6.
/// DO NOT EDIT. Regenerate with: scripts/generate_web_platform5.py

/// Cascade origin.
enum Origin {
  ORIGIN_UNSPECIFIED = 0;
  UA = 1;
  USER = 2;
  AUTHOR = 3;
  ANIMATION = 4;
  TRANSITION = 5;
}

/// Cascade layer.
message CascadeLayer {
  string name = 1;
  repeated CascadeLayer sub_layers = 2;
}

/// Importance level.
enum Importance {
  IMPORTANCE_UNSPECIFIED = 0;
  NORMAL = 1;
  IMPORTANT = 2;
}

/// Scope proximity.
enum ScopeProximity {
  SCOPE_PROXIMITY_UNSPECIFIED = 0;
  NEAR = 1;
  FAR = 2;
}

/// Layer importance (ordered).
message LayerImportance {
  CascadeLayer layer = 1;
  Origin origin = 2;
  Importance importance = 3;
}
