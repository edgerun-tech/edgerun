syntax = "proto3";

package edgerun.v0.css.animations;

/// CSS Animation Types — generated from CSS Animations Level 1.
/// DO NOT EDIT. Regenerate with: scripts/generate_web_platform5.py

/// Animation play state.
enum AnimationPlayState {
  ANIMATION_PLAY_STATE_UNSPECIFIED = 0;
  RUNNING = 1;
  PAUSED = 2;
}

/// Animation fill mode.
enum AnimationFillMode {
  ANIMATION_FILL_MODE_UNSPECIFIED = 0;
  FILL_NONE = 1;
  FILL_FORWARDS = 2;
  FILL_BACKWARDS = 3;
  FILL_BOTH = 4;
}

/// Animation direction.
enum AnimationDirection {
  ANIMATION_DIRECTION_UNSPECIFIED = 0;
  DIRECTION_NORMAL = 1;
  DIRECTION_REVERSE = 2;
  DIRECTION_ALTERNATE = 3;
  DIRECTION_ALTERNATE_REVERSE = 4;
}

/// A single keyframe in an animation.
message Keyframe {
  repeated double offsets = 1; // 0.0 to 1.0 (can be multiple for same keyframe)
  repeated CssDeclaration properties = 2;
}

/// Css property declaration.
message CssDeclaration {
  string property = 1;
  string value = 2;
  bool important = 3;
}

/// Animation keyframes set.
message Keyframes {
  string name = 1;
  repeated Keyframe keyframes = 2;
}

/// A single animation.
message Animation {
  string name = 1;
  optional double duration = 2; // seconds
  optional string easing = 3; // easing function string
  optional double delay = 4; // seconds
  optional uint32 iteration_count = 5; // 0 = infinite
  optional bool infinite = 6;
  optional AnimationDirection direction = 7;
  optional AnimationFillMode fill_mode = 8;
  optional AnimationPlayState play_state = 9;
  optional string timeline = 10;
}
