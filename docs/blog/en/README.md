---
title: EdgeRun Build Log
date: 2026-04-30
author: Ken
summary: Build log of the live Edgerun platform as features become production-ready.
tags: [edgerun, infrastructure, architecture]
---
# EdgeRun Build Log

This build log is not generated by an external CMS and is not served by a third
party stack. It is served from the same checkout used by the platform itself, via
`edgerun-server`.

The page you are reading is part of a single deployment surface that also hosts:

- production email workflows,
- DNS and HTTPS entry points,
- webmail surface,
- public crate/code explorer,
- this exact release log.

That shape is deliberate: the log should describe what already moves the real
service, not a separate “marketing deployment.”

## Current platform achievements

- A compact integrated server process now delivers core networked services with a
  smaller process and image footprint than reference comparators in local benchmarks.
- Runtime and benchmarks are reproducible from repository state, with deterministic
  output paths for evidence directories.
- The blog, git surface, and runtime surfaces are shareable through the same
  release flow.
- Feature boundaries remain explicit: only selected crates and paths are exposed by
  release metadata.

## Where to look next

Posts in this log are used to document public-facing capabilities as they become
reliable enough for day-to-day use. Each entry is intended to connect to a real
code path and a real commit.
