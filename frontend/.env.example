# OpenCode Zen API - Get your key at https://opencode.ai/auth
# The free tier includes MiniMax M2.5 Free model
OPENCODE_API_KEY=your-api-key-here

# Gmail OAuth - Create app at https://console.cloud.google.com/apis/credentials
# Scopes: Gmail read/write, user email
# Redirect URI must match: http://localhost:3000/api/gmail/callback
GMAIL_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your-google-client-secret
GMAIL_REDIRECT_URI=http://localhost:3000/api/gmail/callback

# Browser app catalog trust root.
# Must be the Ed25519 public key, as 32 hex bytes, for the store key that signs
# public/apps/catalog.ecat. The frontend refuses to load the app catalog without
# this value.
NEXT_PUBLIC_EDGERUN_APP_STORE_ID=your-32-byte-store-public-key-hex

# Local catalog publishing only. Do not commit real store seeds.
# Used by scripts/refresh-app-catalog.mjs to sign public/apps/catalog.ecat.
EDGERUN_APP_STORE_SEED=your-32-byte-store-private-seed-hex
