/**
 * @deprecated Use platform/runtime/wasm-registry instead.
 * WASM module caching is managed by wasmRegistry.
 */
export { wasmRegistry, allWasmModules, loadWasmForApp, removeWasm, getWasmUrl, getWasmBytes, validateWasmHash } from "@/platform/runtime/wasm-registry"
